# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows this workflow to be run manually from the Actions tab
  workflow_dispatch:
    
jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

#    - name: Check for up-to-date Maven cache
#      uses: actions/cache@v2
#      id: maven-cache
#      with:
#        path: ~/.m2/repository
#        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
#        restore-keys: |
#          ${{ runner.os }}-maven-
          
    - name: Set up JDK 15
      uses: actions/setup-java@v1
      with:
        java-version: 15
        architecure: x64
        
    - name: Cache Maven packages
      uses: actions/cache@v2
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
        
    - name: Build with Maven
      run: mvn -X -B package --file pom.xml
    
    - name: Output settings
      run: |
        echo "outputting contents of C:\Users\runneradmin\.m2\settings.xml"
        echo ""
        type "C:\Users\runneradmin\.m2\settings.xml"
        echo ""
        echo "calling dir"
        echo ""
        dir
        

    
    # Cache local Maven repository when pom.xml changes
    #- name: Cache local Maven repository if needed
      
      
